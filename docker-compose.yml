version: '3.8'

services:
  postgres-exporter:
    image: docker.arvancloud.ir/prometheuscommunity/postgres-exporter
    container_name: postgres-exporter
    hostname: postgres-exporter
    ports:
      - "9187:9187"
    environment:
      DATA_SOURCE_URI: "postgres:5432/thingsboard?sslmode=disable"
      DATA_SOURCE_USER: "postgres"
      DATA_SOURCE_PASS: "postgres"
    networks:
      - tb-network
      - monitoring
        
  mongo-exporter:
    image: percona/mongodb_exporter:0.40
    container_name: mongodb-exporter
    hostname: mongo-exporter
    command:
      - --mongodb.uri=mongodb://192.168.2.105:27019
      - --collect-all
    expose:
      - 9216
    ports:
      - "9216:9216"
    networks:
      - monitoring
      - fiware-network
    restart: unless-stopped
    labels:
      org.label-schema.group: "monitoring"

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    restart: unless-stopped
    expose:
      - 9090
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    networks:
      - monitoring
    labels:
      org.label-schema.group: "monitoring"

  cadvisor:
    image: gcr.io/cadvisor/cadvisor
    container_name: cadvisor
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /cgroup:/cgroup:ro #doesn't work on MacOS only for Linux
      - /dev/disk/:/dev/disk:ro
    ports:
      - "8089:8080"
    networks:
      - monitoring
      - edgex-network
      - fiware-network
      - tb-network
    restart: unless-stopped
    command:
      - '--housekeeping_interval=10s'
      - '--docker_only=false'
      - '--store_container_labels=true'
      - '--whitelisted_container_labels=com.docker.compose.service,com.docker.compose.project,io.kubernetes.container.name,io.kubernetes.pod.name'
    labels:
      org.label-schema.group: "monitoring"

  redis-exporter:
    image: oliver006/redis_exporter:latest
    container_name: redis-exporter
    hostname: redis-exporter
    ports:
      - "9121:9121"
    environment:
      REDIS_ADDR: "redis://edgex-redis:6379"
      REDIS_PASSWORD: ""
    networks:
      - monitoring
      - edgex-network
    restart: unless-stopped
    labels:
      org.label-schema.group: "monitoring"

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    expose:
      - 9100
    ports:
      - "9100:9100"
    networks:
      - monitoring
    labels:
      org.label-schema.group: "monitoring"


networks:
  monitoring:
    name: monitoring-network
  edgex-network:
    external: true
    name: edgex-network
  tb-network:
    external: true
    name: tb-network
  fiware-network:
    external: true
    name: fiware-network


volumes:
  prometheus_data:
  grafana_data:
